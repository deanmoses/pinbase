{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "franchises.schema.json",
  "title": "Franchises",
  "description": "Licensed or original intellectual properties used as pinball themes",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "slug": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
      "name": { "type": "string", "minLength": 1 },
      "description": { "type": "string" }
    },
    "required": ["slug", "name"],
    "additionalProperties": false
  }
}
